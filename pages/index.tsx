import Head from 'next/head';

const Home = () => {
	return (
		<div>
			<Head>
				<title>Example Next App</title>
				<link rel="icon" href="/favicon.ico" />
			</Head>

			<main>
				Hello!
			</main>
		</div>
	);
};

export default Home;
